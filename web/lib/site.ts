/**
 * The canonical origin for skillrank.dev.
 *
 * The apex 308-redirects to www, so www is the host that actually serves the
 * site. Canonical URLs, OG tags and the sitemap all have to agree with that or
 * every entry costs a redirect hop and splits ranking signals across two hosts.
 */
export const SITE_URL = "https://www.skillrank.dev";
