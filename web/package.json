{
  "name": "skillrank-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "16.3.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  },
  "//overrides": "postcss and sharp reach us only transitively through next, so force patched versions. sharp is next's OPTIONAL image-optimization dep and this app never uses next/image (no next/image import, no images block in next.config.mjs), so the out-of-range bump is inert here.",
  "overrides": {
    "postcss": "^8.5.23",
    "sharp": "^0.35.3"
  },
  "//engines": "sharp >=0.35 requires Node >=20.9.0; pin the floor so the Vercel project's Node version cannot silently drop below it.",
  "engines": {
    "node": ">=20.9.0"
  },
  "license": "Elastic-2.0"
}
