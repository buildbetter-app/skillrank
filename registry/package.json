{
  "name": "skillrank-registry",
  "version": "0.1.0",
  "private": true,
  "description": "api.skillrank.dev — hosted registry serving /v3/rest/skill-registry: catalog reads plus account-gated eval-result ingest",
  "license": "Elastic-2.0",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  }
}
